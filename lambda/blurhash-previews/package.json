{
  "name": "blurhash-previews",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "rm -rf dist && tsc && sls deploy && rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "blurhash": "^1.1.5",
    "mongodb": "^4.2.0",
    "typescript": "^4.6.3"
  }
}
