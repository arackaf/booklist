query allSubjects($publicUserId: String) {
  allSubjects(publicUserId: $publicUserId, SORT: { name: 1 }) {
    Subjects {
      _id
      name
      backgroundColor
      textColor
      path
    }
  }
}
