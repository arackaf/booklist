query GetBookDetails($_id: String, $publicUserId: String) {
  getBook(_id: $_id, cache: 9, publicUserId: $publicUserId) {
    Book {
      editorialReviews {
        source
        content
      }
      similarBooks {
        title
        authors
        smallImage
        asin
      }
    }
  }
}
