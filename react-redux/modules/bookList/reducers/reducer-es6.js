const {  } = require('../actions/actionNames');

const initialState = () => ({
    viewList: []
});

function reducer(state = initialState(), action = {}){
    debugger;
    return state;
}

module.exports = reducer;